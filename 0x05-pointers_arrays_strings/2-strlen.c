#include "main.h"

/**
 * _strlen - calculate the lenth of a string.
 *
 * @: the string to calculate it's length.
 * Return: length of a string.
 */

int _strlen(char *s)
{
	int i;
	int lengtth = 0

		for (i =0; s[i] != '\0'; i++)
		{
		       length++;
		}
	return (length);
}
